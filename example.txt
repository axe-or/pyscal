module my_mod;

var
    x, y : Int = 6, +7 * 3 << (1 / 20) ~ -6 or 6 > 1;
    arr0, arr1, arr_foo: []^[6]bool;
    x, y: ^Int;

const
    hello: bool = true;
    MAX_WIDTH: int = 0;

type
    A, B = ^int, [6]^int;

proc foo(x: Int, bar, baz: Person, ok, nok: bool, skibidi: gyatt): bool;

